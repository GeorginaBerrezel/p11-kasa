// AccommodationDetails.js
import React from "react";

const AccommodationDetails = ({ id }) => {
  // Logique pour afficher les détails de l'hébergement avec l'ID donné

  return (
    <div>
      {/* Contenu de la nouvelle page */}
      <h2>Accommodation Details - {id}</h2>
      {/* Ajoutez le contenu spécifique de la page ici */}
    </div>
  );
};

export default AccommodationDetails;
